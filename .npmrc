# Match the committed pnpm lockfile (built with auto-install-peers disabled).
# Without this, `pnpm install --frozen-lockfile` fails with
# ERR_PNPM_LOCKFILE_CONFIG_MISMATCH on pnpm >= 8 (default is true).
# Settings only — never put auth tokens here.
auto-install-peers=false
